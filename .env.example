# Public values only (safe to commit this file; never put secrets here)
NEXT_PUBLIC_API_URL=https://api.tabi.africa
# Dashboard app (sign in, sign up, workspace) — set when marketing and app are on different hostnames
NEXT_PUBLIC_APP_URL=https://app.tabi.africa
